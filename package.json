{
  "name": "nice88asia-website",
  "version": "1.0.0",
  "description": "Nice88 Asia - Online Casino & Sports Betting Website (nice88asia.com)",
  "scripts": {
    "build": "node build/build.js",
    "build:react": "vite build",
    "images:sync": "node build/sync-source-images.js",
    "images:import": "node build/import-donor-images.js",
    "images:audit": "node build/audit-card-images.js",
    "images:alts": "node build/apply-image-alts.js",
    "serve": "npx serve dist -p 3000",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name=nice88asia",
    "sync": "node scripts/content-sync.js",
    "sync:all": "node scripts/content-sync.js --all",
    "sync:refresh": "node scripts/content-sync.js --refresh",
    "sync:force": "node scripts/content-sync.js --force",
    "sync:daily": "node scripts/content-sync.js --daily",
    "sync:doctor": "node scripts/sync-doctor.js",
    "backfill": "node scripts/backfill-internal-links.js",
    "backfill:force": "node scripts/backfill-internal-links.js --force",
    "audit:links": "node scripts/audit-internal-links.js",
    "rebuild:blogs": "node scripts/rebuild-blog-pages.js",
    "build:blogs": "node build/sync-blog-output.js",
    "indexnow": "node build/submit-indexnow.js"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.2",
    "dotenv": "^16.4.7",
    "sharp": "^0.34.5",
    "vite": "^7.1.3",
    "wrangler": "^4.82.2"
  },
  "dependencies": {
    "ogl": "^1.0.11",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
